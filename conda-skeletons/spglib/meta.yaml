package:
  name: spglib
  version: "1.9.8.11"

source:
  fn: spglib-1.9.8.11.tar.gz
  url: https://pypi.python.org/packages/ff/38/44a1e45afe5951bd86af7113f38bda8e22959826e9364b86b0327add80c0/spglib-1.9.8.11.tar.gz
  md5: ff1d01c94d6ef0695ae875908287eb65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - spglib = spglib:main
    #
    # Would create an entry point called spglib that calls spglib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - spglib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atztogo.github.io/spglib/
  license: MIT
  summary: 'This is the spglib module.'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
